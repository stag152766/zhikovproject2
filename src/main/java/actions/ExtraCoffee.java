package actions;

import entities.Entity;

import java.util.Map;

/**
 * Add coffee to existing map
 */
public class ExtraCoffee implements Action {

    @Override
    public void apply(Map<Entity, Integer[]> map) {
        // get a snapshot
        // get random empty field
        // set new coordinates
    }
}
